🏆 Ctrl+Win — Fullstack-платформа для управления киберспортивными лигами

Ctrl+Win — это профессиональная система автоматизации киберспорта. Проект фокусируется на управлении жизненным циклом турниров: от регистрации участников до автоматизации продвижения по сетке и контроля финансовых потоков.
🚀 Основные возможности

    Automated Bracket Logic: Автоматизация продвижения участников по сетке с использованием рекурсивных связей в БД (next_match_id).

    Financial Accounting: Модуль учета призовых фондов, взносов и спонсорских поступлений на уровне базы данных (Numeric(10, 2)).

    Multi-Format Management: Настройка турниров под форматы 1v1, 5v5 и различные дисциплины.

    Live Analytics: Мгновенный расчет метрик платформы через асинхронные запросы к PostgreSQL.

    Infinite UI Experience: Интерактивный интерфейс с поддержкой масштабирования (Zoom & Pan) для работы с большими объемами данных.

🛠 Технологический стек
Backend (Python)

    FastAPI — высокопроизводительное асинхронное ядро.

    SQLAlchemy 2.0 & PostgreSQL — современный стек для работы с реляционными данными.

    Pydantic v2 — строгая валидация данных (схемы вынесены в schemas.py).

    Docker & Docker Compose — полная контейнеризация всей системы.

Frontend (React)

    Vite — инструмент сборки нового поколения.

    Tailwind CSS — дизайн в стиле Glassmorphism и неоновые акценты.

    SVG Filters — кастомные эффекты свечения для визуальных элементов.

🏗 Архитектура проекта

Проект организован по принципу разделения ответственности (Separation of Concerns):

├── backend/
│   ├── models/       # Слой данных (database.py, models.py)
│   ├── routes/       # API-контроллеры (base, tournaments, users)
│   ├── services/     # Бизнес-логика (расчеты, логика продвижения)
│   ├── schemas.py    # Pydantic-модели для валидации запросов и ответов
│   ├── config.py     # Централизованное управление переменными окружения и настройками БД
│   ├── utils.py      # Вспомогательные функции (хеширование, обработка данных)
│   ├── Dockerfile    # Инструкции по сборке образа бэкенда
│   └── main.py       # Точка входа, настройка lifespan и CORS
├── frontend/
│   ├── src/
│   │   ├── components/ # Переиспользуемые UI-компоненты
│   │   ├── pages/      # Полноэкранные страницы приложения
│   │   ├── styles/     # Глобальные стили, темы и конфигурация Tailwind
│   │   ├── global/     # Константы, API-конфиги и общие функции
│   │   ├── layout/     # Обертки интерфейса (Sidebar, Navbar)
│   │   ├── App/        # Навигация и основная логика React
│   │   └── main.jsx    # Точка входа фронтенда
│   ├── index.html      # Базовый HTML-шаблон
│   ├── package.json    # Зависимости и скрипты фронтенда
│   └── vite.config.js  # Конфигурация сборки и dev-сервера
├── docker-compose.yml  # Оркестрация бэкенда, фронтенда и базы данных
└── README.md
📦 Инфраструктура и развертывание

Мы используем современные инструменты DevOps для обеспечения стабильности:

    Config Management: Файл config.py позволяет безопасно управлять ключами и доступами, не "зашивая" их в код.

    Styles & UI: Папка styles содержит централизованные правила оформления, что позволяет легко менять визуальную тему всего проекта.

    Docker Compose: Позволяет поднять готовую инфраструктуру (API + UI + Database) одной командой:
    Bash

    docker-compose up --build

⚡️ Быстрый старт (Manual)
1. Backend
Bash

cd backend
python -m venv .venv
# Активация: source .venv/bin/activate или .venv\Scripts\activate
pip install -r requirements.txt
uvicorn main:app --reload

2. Frontend
Bash

cd frontend
npm install
npm run dev

📈 Перспективы (Roadmap)

    [x] Асинхронная архитектура и полная контейнеризация.

    [x] Рекурсивная логика продвижения по сетке в БД.

    [ ] Интеграция с внешними API (Challonge/Toornament).

    [ ] Автоматический расчет рейтинга участников.

🤝 Контакты

Разработано для автоматизации киберспортивной индустрии. Проект готов к деплою и масштабированию.
